<svg width="64" height="64" viewBox="0 0 64 64" fill="none" xmlns="http://www.w3.org/2000/svg">
  <!-- Background circle -->
  <circle cx="32" cy="32" r="30" fill="#0a0a0a" stroke="#d4af37" stroke-width="2"/>
  
  <!-- Clock face (time theme) -->
  <circle cx="32" cy="32" r="22" fill="none" stroke="#d4af37" stroke-width="1.5"/>
  
  <!-- Center dot -->
  <circle cx="32" cy="32" r="2.5" fill="#d4af37"/>
  
  <!-- Clock hands showing different times (time travel concept) -->
  <g stroke="#d4af37" stroke-width="2" stroke-linecap="round">
    <!-- Hour hand 1 (pointing to 12) -->
    <line x1="32" y1="32" x2="32" y2="15"/>
    <!-- Minute hand 1 (pointing to 3) -->
    <line x1="32" y1="32" x2="45" y2="32"/>
  </g>
  
  <!-- Arrow symbols (travel/movement) -->
  <g fill="#d4af37">
    <!-- Forward arrow on right -->
    <path d="M 48 24 L 52 28 L 48 32" stroke="#d4af37" stroke-width="1.5" fill="none" stroke-linecap="round" stroke-linejoin="round"/>
    <!-- Backward arrow on left -->
    <path d="M 16 24 L 12 28 L 16 32" stroke="#d4af37" stroke-width="1.5" fill="none" stroke-linecap="round" stroke-linejoin="round"/>
  </g>
  
  <!-- Sparkle/shine effect (luxury) -->
  <g fill="#d4af37" opacity="0.8">
    <circle cx="50" cy="18" r="1.5"/>
    <circle cx="14" cy="48" r="1.5"/>
  </g>
</svg>